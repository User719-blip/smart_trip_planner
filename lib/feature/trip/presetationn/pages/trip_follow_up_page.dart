import 'package:flutter/material.dart';

class TripFollowUpPage extends StatelessWidget {
const TripFollowUpPage({ super.key });

  @override
  Widget build(BuildContext context){
    return Container();
  }
}